<!--pages/index1/index1.wxml-->
<block wx:for="{{products}}">
<van-card
  price="{{item.price}}"
  
  desc="{{item.color}}"
  title="{{item.title}}"
  thumb="{{item.image}}"
  data-id="{{item._id}}"
  bindtap="click"
/>
</block>

